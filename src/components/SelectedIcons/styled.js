import styled from 'styled-components'

export const BlockCirculo = styled.View({
    paddingTop: 10,
    height: 100,
    left: 15,
})

export const Circulo = styled.TouchableOpacity({
    height: 65,
    width: 65,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 50,
    margin: 8,
})
